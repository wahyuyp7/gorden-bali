---
const { title, class: className = "" } = Astro.props;
---

<article class:list={["soft-card p-6", className]}>
  <h2 class="card-title">{title}</h2>
  <div class="mt-3 text-sm leading-relaxed text-neutral-600">
    <slot />
  </div>
</article>
